# Matches the alkanes-rs main pin's toolchain (see TOOLCHAIN.md).
[toolchain]
channel = "1.88.0"
components = ["rustfmt", "clippy", "rust-analyzer"]
targets = ["wasm32-unknown-unknown", "wasm32-wasip1"]
